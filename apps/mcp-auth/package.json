{
	"name": "@oksai/mcp-auth",
	"version": "0.1.0",
	"description": "OAuth 2.0 授权服务器，为 MCP 服务端提供 JWT 令牌和 JWKS 支持",
	"license": "AGPL-3.0",
	"private": true,
	"type": "commonjs",
	"main": "dist/src/main.js",
	"scripts": {
		"build": "rimraf dist && tsc -p tsconfig.app.json",
		"start": "ts-node src/main.ts",
		"start:dev": "tsx watch src/main.ts"
	},
	"dependencies": {
		"@nestjs/common": "catalog:",
		"@nestjs/core": "catalog:",
		"@nestjs/platform-express": "catalog:",
		"bcryptjs": "^3.0.3",
		"express": "^5.2.1",
		"jsonwebtoken": "^9.0.3"
	},
	"devDependencies": {
		"@types/jest": "catalog:",
		"@types/jsonwebtoken": "^9.0.10",
		"jest": "catalog:",
		"ts-jest": "catalog:"
	}
}
